// postcss.config.mjs
const config = {
  plugins: {
    // Change this line:
    "tailwindcss": {},
    // If you need to use autoprefixer (which is highly recommended):
    "autoprefixer": {},
  },
};

export default config;